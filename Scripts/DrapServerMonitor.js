$(document).ready(function () {
    //var canvasServer = document.getElementById('canvasServer');
    //var stageServer = new JTopo.Stage(canvasServer);
    //showJTopoToobar(stageServer);//显示工具栏
    //stageServer.eagleEye.visible = true;
    //var sceneServer = new JTopo.Scene(stageServer);

    //$(function () {
    //    var width_Pscreen = $(window).width();       //获取父级窗口的宽度
    //    var heigth_Pscreen = $(window).height();
    //    console.log("我是服务器画布屏幕高度：" + heigth_Pscreen + ",我是服务器画布屏幕宽度：" + width_Pscreen);
    //    $('#canvasServer').attr("width", $(window).get(0).innerWidth);
    //    $('#canvasServer').attr("height", $(window).get(0).innerHeight);
    //    $('.jtopo_toolbar').css({ 'width': $(window).get(0).innerWidth });
    //});
    //console.log("我是服务器画布");
});